const express=require("express");const cors=require("cors");const app=express();app.use(cors());app.use(express.json());app.get("/health",(req,res)=>{res.json({status:"OK",runtime:"Node.js Express"})});app.get("/",(req,res)=>{res.json({success:true,message:"Node.js API"})});app.listen(process.env.PORT||10000,"0.0.0.0",()=>console.log("API running"));
